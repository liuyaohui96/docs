
## 安装
```shell
# 建议本地安装而不是全局安装，防止因为不通过项目依赖不同版本的webpack而导致冲突，以下为本地安装的命令

#  webpack 4+ 版本，还需要需要安装 CLI
# === npm 安装
npm init
# === 安装最新版本
npm i -D webpack webpack-cli
# === 安装指定版本
npm i -D webpack@<version> webpack-cli

# === yarn 安装
yarn init
yarn add -D webpack webpack-cli
```

