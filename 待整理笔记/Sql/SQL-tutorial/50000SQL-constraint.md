# SQL constraint
```SQL
<constraint>: NOT NULL|DEFAULT

<constraint-function>: (UNIQUE|PRIMARY KEY|FOREIGN KEY)\(<column-list>\)
                       |CHECK(<expression>)


-------appendix
<column-list>:<column>(,\s<column>)*
```