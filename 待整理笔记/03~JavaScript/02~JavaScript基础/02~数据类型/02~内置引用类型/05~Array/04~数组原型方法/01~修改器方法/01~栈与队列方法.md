
## 栈与队列方法
```js
// 将一个或多个元素添加到数组的末尾，并返回该数组的新长度
Array.prototype.push(element1, ..., elementN)
// 删除数组的第一个元素，并返回这个元素
Array.prototype.shift()

// 在数组的开头增加一个或多个元素，并返回数组的新长度
Array.prtotype.unshift(element1, ..., elementN)
// 删除最后一个元素，并返回该元素的值。此方法更改数组的长度
Array.prototype.pop()
```