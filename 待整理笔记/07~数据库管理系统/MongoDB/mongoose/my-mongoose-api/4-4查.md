 find, findById, findOne, or where static methods

 # find
 ```js
const rts = await Person.find({
  name: ctx.query.name
});
```

# findOne
```js
const rts = await Person.find({
  name: ctx.query.name
});
```